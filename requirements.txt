# TFI-banisa — Python dependencies
# Server
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Vector DB (embedded local mode or remote server)
qdrant-client>=1.12

# ML — embeddings + captioning
# transformers pinned <4.50: Florence-2 remote code breaks on newer versions
sentence-transformers>=3.0
transformers>=4.44,<4.50
torch>=2.3
timm>=1.0          # required by Florence-2 remote code
einops>=0.8        # required by Florence-2 remote code

# Image + OCR
pillow>=10.0
pytesseract>=0.3.10

# Transliteration (Roman Telugu -> native script)
indic-transliteration>=2.3

# Tests
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
